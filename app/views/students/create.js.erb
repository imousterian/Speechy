<% if @student.valid? %>

    $(".js-students").prepend("<%= escape_javascript(render(@student)) %>");

<% else %>

    $("#tabs-errors").html("<%= escape_javascript(render 'shared/messages', object: @student) %>");

    removeErrorExplanation();

<% end %>
