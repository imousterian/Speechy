<% if @student.errors.any? %>
    $("#student-modal-body").prepend("<%= escape_javascript(render 'shared/messages', object: @student) %>");
<% else %>
    $("#student-modal").modal("hide");
<% end %>
