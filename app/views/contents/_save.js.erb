$("div.errors").html("")
    <% if @content.errors.any? %>
        $("div.errors").append("<%= escape_javascript(render 'shared/messages', object: @content) %>");
    <% else %>
        $("#content-modal").modal("hide")
    <% end %>
